{
  "name": "tw-crypt",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha --reporter spec --compilers ts:ts-node/register test/index.ts",
    "lint": "tslint -c tslint.json './**/*.ts'",
    "build:watch": "tsc -p tsconfig.json -w --allowSyntheticDefaultImports --alwaysStrict --diagnostics --pretty",
    "build": "tsc -p tsconfig.json -w --allowSyntheticDefaultImports --alwaysStrict --diagnostics --pretty"
  },
  "keywords": [
    "encryption",
    "node",
    "crypto"
  ],
  "author": "John Waweru <wambugu.john.waweru@outlook.com>",
  "license": "MIT",
  "dependencies": {
    "typescript": "^2.2.2"
  },
  "devDependencies": {
    "@types/node": "^7.0.13",
    "mocha": "^3.2.0",
    "ts-node": "^3.0.2",
    "tslint": "^5.1.0",
    "tslint-eslint-rules": "^4.0.0"
  },
  "types": "./index.d.ts"
}
